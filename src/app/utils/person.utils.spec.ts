import { PersonUtils } from './person.utils';

/*
This is not a real working thing, just an example of how to do a unit test.
use: npm run test to run ci

describe('PersonUtils', () => {
  describe('#buildPerson', () => {
    beforeEach(() => {
      this.people = [
        { name: 'Billy', age: 20, hobby: 'Sport' },
        { name: 'Joe', age: 22, hobby: 'Balling' }
      ];
    });

    it('expects to create a person', () => {
      expect(PersonUtils.buildPerson('Billy', 20, 'Sport').toBe({ 'Billy', 20, 'Sport' }));
    });
  });
});
*/
